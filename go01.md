# go01

## 文档定位

`go01.md` 用于记录“检索测试”相关功能从占位页到可用功能的优化思路、开发过程、问题修复和下一步计划。  
这不是一次性变更说明，而是一份阶段性总结文档。

---

## 一、背景与目标

项目原有“检索测试”入口存在两个明显问题：

1. 页面本身是静态演示，不具备真实检索能力
2. 前端多个入口仍然保留“待开发”状态，用户无法稳定进入已实现功能

本阶段的目标是先做一条不依赖大模型、可快速落地的检索链路，让页面真正可用，再为后续“关键词 + 向量 + LLM”扩展预留空间。

本阶段目标拆解如下：

- 将“检索测试”从假数据页面改成真实功能页面
- 支持检索历史对话
- 支持检索已上传文档内容
- 支持从结果直接跳转到对应会话或文档
- 修复聊天服务中影响功能闭环的兼容性问题
- 打通侧边栏和仪表盘入口，使功能真正可达

---

## 二、当前实现结论

当前“检索测试”已从占位功能变成可用功能，具备如下能力：

- 可以通过关键词检索浏览器本地保存的历史会话
- 可以通过关键词检索知识库中已切分入库的文档 chunk 和文件名
- 可以按知识库范围过滤
- 可以在“全部 / 文档 / 会话”三个范围之间切换
- 可以从检索结果直接打开聊天会话
- 可以从检索结果直接打开文档详情
- 可以从侧边栏和仪表盘快捷卡片进入检索页

换句话说，这个功能现在已经从“内部演示页”进入“用户可以直接使用的页面”阶段。

---

## 三、开发记录

### 阶段 1：后端补齐纯关键词检索接口

目标：
- 不依赖 LLM
- 不依赖额外 rerank
- 尽快让知识库内容可被真实搜索

主要文件：
- `backend/Database/milvus_server/milvus_api.py`

完成内容：
- 增加关键词标准化逻辑
- 增加关键词命中评分逻辑
- 增加片段截取逻辑
- 新增 `KeywordSearchRequest`
- 新增 `milvus_service.keyword_search_documents(...)`
- 新增 `POST /search_keyword`

设计选择：
- 直接复用当前 Milvus 中已入库的 `chunk_text`、`filename`、`metadata`
- 不重新建表，不新增独立搜索服务
- 优先做“能跑通”的文档关键词搜索

结果：
- 文档检索从“前端假数据”变成“后端真实返回”

### 阶段 2：前端检索页落地

目标：
- 将 `RetrievalTest` 从静态页面改造成真实交互页

主要文件：
- `frontend/components/RetrievalTest.tsx`
- `frontend/src/i18n/messages/zh-CN.ts`
- `frontend/src/i18n/messages/en-US.ts`

完成内容：
- 支持输入关键词发起检索
- 支持知识库过滤
- 支持“全部 / 文档 / 会话”范围切换
- 文档结果来自 `/search_keyword`
- 会话结果来自浏览器 `localStorage` 中的 `chat_sessions`
- 支持关键词高亮
- 支持展示摘要、知识库名、页码、命中类型、命中关键词等信息

设计选择：
- 历史会话先用本地 `localStorage` 搜索，不引入后端会话存储改造
- 前端先承担会话检索职责，减少本阶段改动范围

结果：
- 页面本身已具备实际检索能力

### 阶段 3：检索结果联动跳转

目标：
- 搜索结果不能只停留在展示层，需要能进入真实业务页面

主要文件：
- `frontend/App.tsx`
- `frontend/components/Chat.tsx`

完成内容：
- 从检索结果跳转到聊天页并打开对应会话
- 从检索结果跳转到知识库文档详情页并打开对应文档
- 修正聊天页默认知识库初始化逻辑，避免打开历史会话时被首个知识库覆盖

结果：
- “搜索 -> 打开目标内容”链路打通

### 阶段 4：聊天服务兼容性修复

目标：
- 修复检索页虽可用，但聊天链路仍可能中断的问题

主要文件：
- `backend/chat/kb_chat.py`
- `部署文档.md`

修复点 1：
- `AsyncOpenAI` 与当前 `httpx` 版本兼容性问题
- 原错误表现：
  - `AsyncClient.__init__() got an unexpected keyword argument 'proxies'`

处理方式：
- 显式注入自建 `httpx.AsyncClient`
- 避免 OpenAI SDK 内部走旧参数路径

修复点 2：
- `MODEL_URL` 未带 `/v1` 时，聊天服务可能请求到站点首页而非 OpenAI 兼容接口
- 原错误表现：
  - 流式返回只有 `sources` 和 `metadata`
  - 没有任何 `content`
  - 非流式可能报 `'str' object has no attribute 'choices'`

处理方式：
- 在 `kb_chat.py` 中增加 OpenAI 兼容地址归一化逻辑
- 自动将 `https://host` 归一化为 `https://host/v1`
- 同步更新默认配置返回值与部署文档说明

结果：
- 对话链路恢复可用
- 检索与聊天的整体闭环稳定下来

### 阶段 5：入口联通

目标：
- 解决“功能已经实现，但用户入口仍显示待开发”的问题

主要文件：
- `frontend/components/Sidebar.tsx`
- `frontend/components/Dashboard.tsx`

完成内容：
- 侧边栏中的“检索测试”去掉禁用状态和“待开发”标记
- 仪表盘“测试检索”快捷卡片绑定到 `retrieval` 页面

结果：
- 用户现在可以从两个主要入口进入检索页
- 功能真正从“可实现”变成“可访问”

---

## 四、当前功能结构

### 1. 文档检索链路

用户输入关键词  
-> 前端 `RetrievalTest.tsx` 调用 `/search_keyword`  
-> `milvus_api.py` 在已有 chunk 中做关键词匹配  
-> 返回命中片段、文件名、页码和 metadata  
-> 前端渲染结果并支持打开文档

### 2. 会话检索链路

用户输入关键词  
-> 前端读取 `localStorage.chat_sessions`  
-> 在标题和消息内容中做关键词匹配  
-> 返回命中摘要和对应会话 ID  
-> 跳转聊天页并打开会话

### 3. 聊天调用链路

前端聊天页  
-> `backend/chat/kb_chat.py`  
-> Milvus 召回知识库文档  
-> 构建上下文  
-> 调用 OpenAI 兼容模型接口  
-> 返回流式或非流式答案

---

## 五、已完成验证

### 静态验证

```bash
python -m py_compile backend/Database/milvus_server/milvus_api.py
python -m py_compile backend/chat/kb_chat.py
cd frontend && npm run build
```

结果：
- Python 编译通过
- 前端 TypeScript 与 Vite 构建通过

### 运行期验证

已验证内容：
- `/search_keyword` 能返回真实文档结果
- 检索页能展示真实搜索结果
- 检索页能跳转到聊天页和文档详情页
- 聊天服务修复后可恢复正常文本输出
- 仪表盘和侧边栏入口已可进入检索页

---

## 六、当前边界

当前实现虽然可用，但仍然有边界，需要明确：

### 1. 历史会话仍是本地搜索

目前历史对话检索依赖前端浏览器本地 `localStorage`：

- 只能检索当前浏览器中的历史会话
- 不支持多端共享
- 不支持后端统一存储
- 不支持跨用户检索

### 2. 文档检索仍是关键词优先

当前文档检索是纯关键词匹配，不是语义检索：

- 对精确词命中比较友好
- 对同义表达、模糊问法支持有限
- 还没有和向量召回融合成统一策略

### 3. 检索页与聊天页仍是两套链路

目前：
- 检索测试页用于快速查找
- 聊天页用于 RAG 问答

这两者已经打通跳转，但还没有统一成“搜索与问答一体化”的单页面工作流。

---

## 七、这次优化的核心价值

这轮改动的价值不在于“又加了一个页面”，而在于把项目里原本分散、半成品、不可达的部分收敛成了一条真正可用的业务链：

- 检索不再是演示，而是能查到真实内容
- 入口不再是占位，而是可以真正进入功能
- 结果不再只是列表，而是可以进入目标对象
- 聊天不再因兼容性问题中断，检索后的后续问答可继续工作

从产品状态看，项目已经从“界面搭建阶段”推进到“功能可试用阶段”。

---

## 八、后续优化建议

建议按优先级推进：

### 优先级 A：补后端会话持久化

目标：
- 把 `chat_sessions` 从 `localStorage` 提升为服务端存储

收益：
- 支持跨浏览器、跨设备、跨用户会话检索
- 检索页与仪表盘最近会话都可统一读后端
- 为后续权限、审计、统计打基础

### 优先级 B：引入混合检索

目标：
- 将当前关键词检索与现有向量召回结合

建议方式：
- 关键词搜索保留为高速通道
- 向量搜索作为语义补充
- 在检索页增加“关键词 / 向量 / 混合”模式切换

收益：
- 兼顾精确命中与语义召回
- 更接近正式 RAG 系统的检索体验

### 优先级 C：统一搜索与问答入口

目标：
- 把“检索测试”和“聊天”从两个分离页面逐步整合

思路：
- 搜索结果支持一键“基于这些结果继续提问”
- 聊天页支持直接查看和筛选召回结果
- 最终形成“查找 -> 阅读 -> 追问”的连续体验

### 优先级 D：增加检索可观测性

目标：
- 让检索过程可分析、可调参

可以增加：
- 检索耗时拆分
- 命中来源统计
- 常见查询词统计
- 搜索无结果记录
- 参数对比能力

---

## 九、结论

`go01` 这一阶段的核心成果，是把“检索测试”从一个待开发入口和静态展示页，推进成了一个有真实后端支撑、有结果联动、有入口可达、有聊天链路兜底的可用功能。

后续工作重点不再是“从 0 到 1 做出来”，而是围绕以下三个方向继续优化：

1. 从本地检索升级到服务端可共享检索
2. 从纯关键词检索升级到混合检索
3. 从分离式页面升级到统一式搜索问答工作流
